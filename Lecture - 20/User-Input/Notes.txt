User Input is taken by using "prompt" method.
Promt will take the input in String Format.

We can typecast this datatype :
    parseInt() : String -> Number
    parseFloat() : String -> Float point Number.